# Milvus Lite (embedded vector DB). 2.6.10 breaks the unix-socket address; 3.x is untested here.
pymilvus[milvus-lite]>=2.6.0,<3.0,!=2.6.10
setuptools>=70.0,<82.0          # milvus-lite still imports pkg_resources
# MLX embedding stack. mlx-embeddings 0.1.x requires transformers 5 + mlx-vlm; pin until validated.
mlx>=0.30.0,<1.0
mlx-lm>=0.28.0,<1.0
mlx-embeddings==0.0.5
transformers>=4.57,<5.0
huggingface_hub>=0.25
# MCP + HTTP server
mcp>=1.10,<2.0
starlette
uvicorn
httpx
watchdog>=4.0.0
